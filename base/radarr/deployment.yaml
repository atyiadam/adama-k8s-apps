apiVersion: apps/v1
kind: Deployment
metadata:
  name: radarr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: radarr
  template:
    metadata:
      labels:
        app: radarr
    spec:
      containers:
      - name: radarr
        image: linuxserver/radarr
        env:
        - name: PUID
          value: "1057"  
        - name: PGID
          value: "1056"  
        volumeMounts:
        - name: config
          mountPath: /config
        - name: config-old
          mountPath: /config-old
        - name: data
          mountPath: /data
        ports:
        - name: http
          containerPort: 7878
      volumes:
        - name: data
          nfs:
            server: your-nfs-server
            path: /your/path/to/data
        - name: config-old
          persistentVolumeClaim:
            claimName: radarr-config-old
        - name: config
          persistentVolumeClaim:
            claimName: radarr-config
