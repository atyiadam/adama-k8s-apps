---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - persistentvolumeclaim.yaml
  - service.yaml
  - serviceaccount.yaml
  - statefulset.yaml
  - ciliumnetworkpolicy.yaml

labels:
  - pairs:
      app: seerr
